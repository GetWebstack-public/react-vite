apiVersion: v1
kind: Service
metadata:
  name: react-vite
  namespace: react-vite
  labels:
    app: react-vite
spec:
  type: ClusterIP
  selector:
    app: react-vite
  ports:
  - port: 80
    targetPort: 5173
    protocol: TCP
    name: http
